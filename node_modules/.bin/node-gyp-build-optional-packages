#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../lmdb/node_modules/node-gyp-build-optional-packages/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../lmdb/node_modules/node-gyp-build-optional-packages/bin.js" "$@"
  ret=$?
fi
exit $ret
